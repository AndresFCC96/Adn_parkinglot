jasypt:
   encryptor:
      password: ${jasyptpwd:secretkey}
      algorithm: PBEWithMD5AndDES

server:
   port: 2020
   servlet:
      context-path: /Parqueadero
      session:
         timeout: 21600s

spring:
   datasource:
      driver-class-name: 
      type: com.zaxxer.hikari.HikariDataSource
      jdbcUrl: jdbc:postgresql://127.0.0.1:5432/adn
      username: postgres
      password: ENC(3TDIsa8fTWZI08UiriSq00NFax6AOTQK)
      hikari:
         poolName: pool
